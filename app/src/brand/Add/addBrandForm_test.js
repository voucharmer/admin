'use strict';
//TODO IMPLEMENTATION
describe('myApp.addBrand module', function() {

  beforeEach(module('myApp.addBrand'));

  describe('addBrand controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addBrandFormCtrl = $controller('addBrandFormCtrl');
      expect(addBrandFormCtrl).toBeDefined();
    }));

  });
});